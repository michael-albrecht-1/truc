import styled from 'styled-components';
import { white } from '../../styles/shared'
import { StyledH1 } from '../../styles/shared';

export const StyledLoginContent = styled.div`
  flex-direction: column;
  color: ${white};
`
export const StyledLoginTitle = styled(StyledH1)``;
